{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "docker run -u $UID -ti --rm -v $PWD:/th -w /th aergus/latex make",
            "problemMatcher": []
        }
    ]
}